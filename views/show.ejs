<% include partials/headersub %>
    <div id="main" class="container">
        <h2><%=place.topic%></h2>
        <img src="<%= place.image %>" alt="">
        <%= place.content.toString().replace(/"/gi,"") %>
    </div>

<% include partials/footer %>